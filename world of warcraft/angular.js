var world = angular.module("world",[])
world.controller("blizzard",function($rootscope)$http){
	$scope.blizzard =[];
	for (var i =  - 1; i >= 0; i--) {
		$http({
			method:GE
		 url:""
		
		})
		}
